id: HRR-BIZ-01-MAP
topic: HRR-BIZ-01
status: outline
kind: recon
title: "The rules the workflow claims to enforce"
objective: >-
  Record each rule the workflow states or implies, and where the decision that
  enforces it is taken, since a rule decided in the client is not enforced
  anywhere.

requires:
  all_of: [recon.workflow.mapped]
