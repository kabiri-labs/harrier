id: PTN-PRT-02-HEADER
topic: PTN-PRT-02
status: outline
role: stage
title: "Trusted request-supplied host"
objective: >-
  Determine whether a Host or forwarding header the tester sets changes the
  host the application composes into its output.

surfaces:
  any_of: [reverse-proxy]

requires:
  all_of: [recon.entrypoints.mapped]
  any_of: [access.anon, access.user]
yields: [surface.host.controlled]
closes: [surface.host.controlled]
