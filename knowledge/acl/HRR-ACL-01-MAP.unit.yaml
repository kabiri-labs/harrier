id: HRR-ACL-01-MAP
topic: HRR-ACL-01
status: outline
role: stage
kind: recon
title: "The roles and the accounts held for each"
objective: >-
  Record every role the application distinguishes and which test account
  exercises each, since an access-control finding is only as strong as the
  pair of accounts that produced it.

requires:
  all_of: [access.user]
yields: [recon.roles.mapped]
