id: HRR-ACL-06-MAP
topic: HRR-ACL-06
status: outline
role: stage
kind: recon
title: "The fields the model carries beyond those the form submits"
objective: >-
  Record which fields the object model holds that the interface never submits,
  since those are the ones a binding failure exposes.

requires:
  all_of: [access.user]
