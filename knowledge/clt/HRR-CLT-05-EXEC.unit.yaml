id: HRR-CLT-05-EXEC
topic: HRR-CLT-05
status: outline
title: "Execution of a substituted resource"
objective: >-
  Determine whether a substituted location is loaded as script or as a
  template, so its content executes rather than being displayed.

requires:
  all_of: [primitive.resource.load]
yields: [primitive.exec.client]
